// Implementation moved inline into aureader.h to avoid needing a separate
// translation unit. This file intentionally left minimal so build systems
// that still reference it don't pull duplicate symbols.

#include "aureader.h"

// No out-of-class definitions here.
